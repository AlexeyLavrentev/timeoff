{{t "emails.leaveRequest.subjectNew"}}
=====
<p>{{t "emails.greeting" name=(full_name approver)}}</p>

<p>{{t "emails.leaveRequest.newRequest" requester=(full_name requester)}} <strong>{{>leave_request_detail leave=leave }}</strong>.</p>

{{> email/leave_comments comments=comments }}

{{> email/requester_allowance requester=requester requesterAllowance=requesterAllowance}}

<p>{{t "emails.leaveRequest.pendingRequests"}} {{> email/link href=(concatenate (get_application_domain) '/requests/' ) text=(t "emails.leaveRequest.pendingRequestsLink")}}.</p>

<p>{{t "emails.thanks"}}</p>
