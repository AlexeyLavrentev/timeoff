{{# with leave ~}}
  {{~# if this.get_leave_type_name ~}}{{~ this.get_leave_type_name }} {{/if~}}
  {{t "common.from"}} {{# with this.get_start_leave_day ~}}
    {{~ as_date this.date ~}}
    {{~# if this.is_morning_leave}} ({{t "leave.morning"}}){{/if~}}
    {{~# if this.is_afternoon_leave}} ({{t "leave.afternoon"}}){{/if~}}
  {{~/with}} {{t "common.to"}} {{# with this.get_end_leave_day ~}}
    {{~ as_date this.date ~}}
    {{~# if this.is_morning_leave}} ({{t "leave.morning"}}){{/if~}}
    {{~# if this.is_afternoon_leave}} ({{t "leave.afternoon"}}){{/if~}}
  {{~/with~}}
{{~/with~}}
